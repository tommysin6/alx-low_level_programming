#include <ctype.h>
/**
 * _islower - a function that checks for lowercase character.
 *
 * @c: collect alphabets
 * Return: (1) if c is lower otherwise (0)
 *
 */
int _islower(int c)
{
	return islower(c);
}
